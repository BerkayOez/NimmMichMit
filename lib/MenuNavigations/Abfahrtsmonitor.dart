import 'package:flutter/material.dart';

class Abfahrtsmonitor extends StatelessWidget {
  const Abfahrtsmonitor({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Abfahrtsmonitor"),
        backgroundColor: Colors.red,
      ),
    );
  }
}