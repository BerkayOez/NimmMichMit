import 'package:flutter/material.dart';

class Netzplaene extends StatelessWidget {
  const Netzplaene({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Netzpläne"),
        backgroundColor: Colors.red,
      ),
    );
  }
}

